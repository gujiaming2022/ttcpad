name: Unite Fonts

on: workflow_dispatch


jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3

    - name: Unite
      run: .\ttf2ttc\unitettc.exe

    - uses: actions/upload-artifact@v3
      with:
        name: time2.ttf 111.ttc
        path: ${{ github.workspace }}
